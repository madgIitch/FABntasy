import { db } from "../../../../../../src/server/db";
import { requireSocialActor, socialError } from "../../../../../../src/server/social-league-http";
const escape=(value:string)=>value.replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&apos;"}[char]!));
export async function GET(_:Request,{params}:{params:Promise<{leagueId:string;eventId:string}>}){try{const actor=await requireSocialActor();const {leagueId,eventId}=await params;const profile=await db.userProfile.findUnique({where:{authUserId:actor.authUserId},select:{id:true}});const event=profile?await db.leagueActivityEvent.findFirst({where:{id:eventId,leagueId,league:{memberships:{some:{userProfileId:profile.id,status:"ACTIVE"}}}},include:{league:{select:{name:true}}}}):null;if(!event)return new Response("Not found",{status:404});const payload=event.payload as {affectedName?:string;magnitude?:number};const title=event.type.replaceAll("_"," ");const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630" role="img" aria-label="Tarjeta Canastio"><rect width="1200" height="630" fill="#101714"/><circle cx="1080" cy="100" r="220" fill="#b8ff45" opacity=".18"/><text x="72" y="90" fill="#b8ff45" font-family="system-ui" font-size="30" font-weight="700">CANASTIO · ${escape(event.league.name)}</text><text x="72" y="260" fill="white" font-family="system-ui" font-size="64" font-weight="800">${escape(title)}</text><text x="72" y="350" fill="white" font-family="system-ui" font-size="44">${escape(payload.affectedName??"Hito de liga")}</text>${payload.magnitude!==undefined?`<text x="72" y="450" fill="#b8ff45" font-family="system-ui" font-size="58" font-weight="800">${escape(new Intl.NumberFormat("es-ES").format(payload.magnitude))}</text>`:""}<text x="72" y="570" fill="#a8b2ad" font-family="system-ui" font-size="24">Datos publicados · Sin acceso privado incluido</text></svg>`;return new Response(svg,{headers:{"content-type":"image/svg+xml; charset=utf-8","content-disposition":`attachment; filename="canastio-${event.id}.svg"`,"cache-control":"private, no-store","x-content-type-options":"nosniff"}});}catch(error){return socialError(error);}}
