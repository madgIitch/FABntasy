import { changePassword } from "../../security-actions"; import { SettingsPage } from "../../settings-header";
export default function PasswordPage(){return <SettingsPage title="Cambiar contraseña" backHref="/app/perfil/seguridad"><form action={changePassword} className="profile-form focused-form"><label>Contraseña actual<input name="currentPassword" type="password" autoComplete="current-password" minLength={8} required/></label><label>Nueva contraseña<input name="password" type="password" autoComplete="new-password" minLength={8} required/></label><label>Repite la contraseña<input name="passwordConfirm" type="password" autoComplete="new-password" minLength={8} required/></label><button className="primary-action">Cambiar contraseña</button></form></SettingsPage>}
