const CACHE="canastio-shell-v16-1",PUBLIC_ASSETS=["/offline","/manifest.webmanifest","/icon-192.png","/icon-512.png"];
self.addEventListener("install",event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(PUBLIC_ASSETS))));
self.addEventListener("activate",event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key))))));
self.addEventListener("fetch",event=>{if(event.request.method!=="GET")return;const url=new URL(event.request.url);if(url.origin!==self.location.origin)return;if(event.request.mode==="navigate"){event.respondWith(fetch(event.request).catch(()=>caches.match("/offline")));return}if(PUBLIC_ASSETS.includes(url.pathname))event.respondWith(caches.match(event.request).then(hit=>hit||fetch(event.request))) });
function safeDestination(value){if(typeof value!=="string"||!value.startsWith("/app")||value.startsWith("//")||value.includes("\\")||/[\u0000-\u001f]/.test(value))return"/app";try{const parsed=new URL(value,self.location.origin);if(parsed.origin!==self.location.origin||(parsed.pathname!=="/app"&&!parsed.pathname.startsWith("/app/"))||/%2e/i.test(value))return"/app";return parsed.pathname+parsed.search+parsed.hash}catch{return"/app"}}
self.addEventListener("push",event=>{let data={};try{data=event.data?.json()||{}}catch{}const destination=safeDestination(data.destination);event.waitUntil(self.registration.showNotification(String(data.title||"Canastio"),{body:String(data.body||"Tienes una novedad"),tag:String(data.tag||"canastio"),icon:"/icon-192.png",badge:"/icon-192.png",data:{destination}}))});
self.addEventListener("notificationclick",event=>{event.notification.close();const destination=safeDestination(event.notification.data?.destination);event.waitUntil(clients.matchAll({type:"window",includeUncontrolled:true}).then(async windows=>{const client=windows.find(candidate=>"focus" in candidate);if(client){if("navigate" in client)await client.navigate(destination);return client.focus()}return clients.openWindow(destination)}))});
