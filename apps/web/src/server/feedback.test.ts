import { beforeEach,describe,expect,it,vi } from "vitest";import { parseFeedback } from "./feedback";
vi.mock("./db",()=>({db:{}}));
describe("feedback contract",()=>{beforeEach(()=>vi.unstubAllEnvs());it("validates bounded categories and text",()=>expect(parseFeedback({category:"BUG",text:"Something broke",idempotencyKey:"abcdefgh"})).toMatchObject({category:"BUG",technicalContextConsent:false}));it("requires explicit consent and strips query/private context",()=>{vi.stubEnv("CANASTIO_RELEASE","r1");expect(parseFeedback({category:"IDEA",text:"Useful idea",idempotencyKey:"abcdefgh",technicalContextConsent:true,route:"/app/league?token=secret",userId:"private"}).technicalContext).toEqual({route:"/app/league",release:"r1"})});it("rejects unbounded or malformed input",()=>expect(()=>parseFeedback({category:"SECRET",text:"x",idempotencyKey:"x"})).toThrow("INVALID_FEEDBACK"))});
