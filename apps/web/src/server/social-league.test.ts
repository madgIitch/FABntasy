import { describe,expect,it } from "vitest";
import { decodeActivityCursor,encodeActivityCursor,isLeagueEventType,isLeagueReaction,orderedLiveRevision,rivalryReason,sharedRoundRange } from "../../../../packages/domain/social-league";
describe("social league contracts",()=>{it("keeps cursor ordering information opaque and stable",()=>{const date=new Date("2026-09-15T10:00:00Z"),cursor=encodeActivityCursor(date,"event-b");expect(cursor).not.toContain("event-b");expect(decodeActivityCursor(cursor)).toEqual({occurredAt:date,id:"event-b"});expect(decodeActivityCursor("broken")).toBeNull()});it("rejects client-defined event types and reactions",()=>{expect(isLeagueEventType("CLAUSE_EXECUTED")).toBe(true);expect(isLeagueEventType("COMMENT_POSTED")).toBe(false);expect(isLeagueReaction("🔥")).toBe(true);expect(isLeagueReaction("texto libre")).toBe(false)});it("compares only shared published rounds",()=>expect(sharedRoundRange([1,2,4],[2,3,4])).toEqual({from:2,to:4,rounds:[2,4]}));it("explains rivalries deterministically",()=>{expect(rivalryReason({positionGap:1,pointsGap:4.5,sharedRounds:4})).toContain("4.5 puntos");expect(rivalryReason({positionGap:4,pointsGap:4,sharedRounds:4})).toBeNull()});it("ignores out-of-order live revisions",()=>{const current={revision:4,value:10};expect(orderedLiveRevision(current,{revision:3,value:99})).toBe(current);expect(orderedLiveRevision(current,{revision:5,value:11}).value).toBe(11)})});
